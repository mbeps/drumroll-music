/**
 * Maximum allowed file size for artist profile images (2 MB).
 * Shared between ArtistImageFileSchema (client) and any server-side guards.
 *
 * @author Maruf Bepary
 */
export const ARTIST_IMAGE_MAX_SIZE_BYTES = 2 * 1024 * 1024; // 2MB
